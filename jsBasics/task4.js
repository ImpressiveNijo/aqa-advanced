// 4.1
let radius = 0.3;
let area_circle = Math.PI*radius**2;
console.log(area_circle.toFixed(2));

// 4.2
let lenght = 0.10;
let wight = 0.4;
let area_rectangle = lenght*wight;
console.log(area_rectangle.toFixed(2));

// 4.3
let radiuscircle = 0.25;
let height = 0.20;
let cylinder_volume = Math.PI*radius**2*height;
console.log(cylinder_volume.toFixed(2));